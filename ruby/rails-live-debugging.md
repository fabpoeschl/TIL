# How to debug a Rails application in the browser

1. Add `raise` at desired point to debug.

2. Start application in terminal.

3. Trigger action to hit breakpoint.

4. Browser opens window with terminal for debugging.
